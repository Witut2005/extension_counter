import Arg from './Arg';
declare class Switch extends Arg {
    private argValues;
    static isSwitch(arg: string): boolean;
    constructor(argName: string, argValues?: string[]);
    get values(): string[];
    set values(values: string[]);
    hasValues(): boolean;
}
export default Switch;
